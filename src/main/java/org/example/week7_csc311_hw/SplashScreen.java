package org.example.week7_csc311_hw;

import javafx.fxml.FXML;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.scene.control.ProgressIndicator;

import java.io.IOException;

public class SplashScreen {

    /*@FXML
    private ImageView logoImage;

    @FXML
    private ProgressIndicator loadingIndicator;

    @FXML
    private void initialize() {
        // Optionally load a logo image
        logoImage.setImage(new Image("org/example/week7_csc311_hw/SUNY_Farmingdale.png")); // logo path

        // Start a thread to simulate loading and then switch to the Login UI
        new Thread(() -> {
            try {
                Thread.sleep(3000); // Simulate loading for 3 seconds
                switchToLogin();
            } catch (InterruptedException | IOException e) {
                e.printStackTrace();
            }
        }).start();
    }

    private void switchToLogin() throws IOException {
        App.setRoot("login"); // Load the Login UI
    }

     */
}
